#include "main.h"
/**
 * print_most_numbers - Write a function that prints the numbers, from 0 to 9
 * Except 2 and 4
 *
 * Return: Void
 */
void print_most_numbers(void)
{
	int i;

	for (i = 48; i <= 57; i++)
	{
		if (i != 50 && 52)
			_putchar(i);
	}
	_putchar('\n');
}
